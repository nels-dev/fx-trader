import {createTheme} from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#557629",
    },
    secondary: {
      main: "#4A2976"
    }
  }
})

export default theme;