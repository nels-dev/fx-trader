import { useAlert } from "../provider/alert.provider";
import { useEffect } from "react";

const Dashboard = () => {
    // const {doAlert} = useAlert()
    // useEffect(()=>{
    //     doAlert({message: 'Hi there, this is a test', type: 'success', title: 'Welcome back'})
    // },[])
    return ( <>
    Dashboard
    </> );
}
 
export default Dashboard;